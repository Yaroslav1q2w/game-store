export const categorySelector = (state) => state.filter.categoryID;
export const pageCountSelector = (state) => state.filter.pageCount;
export const currentLimitSelector = (state) => state.filter.currentLimit;
